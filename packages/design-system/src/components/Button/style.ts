import MuiButton from '@mui/material/Button';
import { styled } from '@mui/material/styles';
import { spacing } from '@raisin/design-tokens';

export const Button = styled(MuiButton)({
  margin: spacing.lg,
});
